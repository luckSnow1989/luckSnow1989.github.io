---
sort: 3
---
# 3.Agent开发

- [大模型必知的5种agent模式](https://mp.weixin.qq.com/s/yb38QQzdxdKHXd5ohh652A)
- [AI Agent（智能体）产业链全解](https://mp.weixin.qq.com/s/4iPY9tHwAMUxQ62toIFwAw)

## 基础概念

### 1.1 定义

AI Agent（人工智能代理）是一个能够自主感知环境、做出决策并执行行动的智能程序。

与传统的"输入→输出"式程序不同，Agent 拥有一个核心特征：自主性。

用一个生活类比来理解：

|      | 传统程序         | AI Agent           |
|------|--------------|--------------------|
| 行为模式 | 像自动售货机：投币→出货 | 像私人助理：理解需求→自主规划→执行 |
| 决策方式 | 预设规则，固定流程    | 根据目标动态调整策略         |
| 适应能力 | 遇到意外就报错      | 遇到障碍会尝试替代方案        |
| 例子   | 计算器          | Siri / AutoGPT     |

### 1.2 AI Agent 的核心组件

一个完整的 AI Agent 由 5 个核心模块 组成：

```
┌─────────────────────────────────────────┐
│              AI Agent 架构               │
│                                         │
│  ┌───────────┐    ┌───────────────────┐ │
│  │  感知模块  │───→│   大脑（LLM）     │ │
│  │ (Perception)│   │  推理 + 决策中心  │ │
│  └───────────┘    └────────┬──────────┘ │
│                            │            │
│  ┌───────────┐    ┌───────▼──────────┐  │
│  │  记忆模块  │◄──→│   规划模块       │  │
│  │ (Memory)   │    │  (Planning)      │  │
│  └───────────┘    └───────┬──────────┘  │
│                            │            │
│                   ┌────────▼──────────┐  │
│                   │   工具模块        │  │
│                   │  (Tools/Actions)  │  │
│                   └──────────────────┘  │
└─────────────────────────────────────────┘
```

1. 感知模块（Perception） — Agent 的"眼睛和耳朵"
   - 接收用户输入、环境变化、外部信号
   - 例：接收一条用户消息、读取一个文件、监听 API 回调
2. 大脑（LLM — 大语言模型） — Agent 的"思考中枢"
   - 理解意图、推理分析、生成方案
   - 例：GPT-4、Claude、通义千问等大模型
3. 记忆模块（Memory） — Agent 的"笔记本"
   - 短期记忆：当前对话上下文（类似工作记忆）
   - 长期记忆：持久化存储的知识和经验（类似长期知识库）
   - 例：向量数据库存储的历史交互记录
4. 规划模块（Planning） — Agent 的"策略师"
   - 将复杂目标拆解为可执行步骤
   - 常见策略：ReAct（推理+行动交替）、Chain-of-Thought（思维链）
   - 例："帮我部署网站" → 拆分为：选服务器→写代码→配置域名→上线
5.  工具模块（Tools/Actions） — Agent 的"手脚"
   - 搜索引擎、代码执行器、文件读写、API 调用等
   - 例：调用天气 API 查询、执行 Python 代码、操作数据库

### 1.3 AI Agent 的真实应用场景

| 行业      | 应用场景  | Agent 做了什么           |
|---------|-------|----------------------|
| 🛒 电商   | 智能客服  | 自主查询订单、处理退款、推荐商品     |
| 💻 软件开发 | 编程助手  | 读取代码→分析Bug→编写修复→运行测试 |
| 📊 金融   | 投研分析  | 自动抓取财报→数据分析→生成研报     |
| 🏥 医疗   | 辅助诊断  | 读取病历→检索文献→给出建议方案     |
| 📚 教育   | 个性化辅导 | 评估学生水平→定制学习路径→互动答疑   |
| 🏠 智能家居 | 场景联动  | 感知温度/光线→自主调节空调/灯光    |

## 工作模式

AI Agent 根据不同的决策方式、架构设计和协作方式，可以分为以下几大类模式：

💡 实际开发中模式经常组合，比如：ReAct决策 + 多Agent协作 + Ecomode执行。选择取决于任务复杂度、成本和精度要求。

一、决策方式（Agent 怎么"想"）

| 模式            | 一句话          | 流程            | 案例                                |
|---------------|--------------|---------------|-----------------------------------|
| ReAct ⭐最主流    | 想→做→看→想→做... | 推理→行动→观察→循环   | LangChain ReAct Agent、Claude Code |
| CoT 思维链       | 一步步想，不动手     | 问题→推理1→推理2→答案 | 适用场景 ：数学推理、逻辑分析、多步推导              |
| ToT 思维树       | 多条路选最优       | 生成多方案→评估→选最佳  | 适用场景 ：创意生成、复杂决策、博弈问题              |
| Reflection 反思 | 自己改自己        | 生成→批评→改进→再批评  | 代表实现 ：Reflexion、Self-Refine       |

二、架构模式（Agent 怎么"组织"）

| 模式              | 一句话       | 结构           | 案例                                                                                      |
|-----------------|-----------|--------------|-----------------------------------------------------------------------------------------|
| 单Agent          | 一个人干所有事   | 用户 ↔ 一个Agent | 适用场景 ：简单问答、单一任务处理                                                                       |
| Pipeline 流水线    | 一个接一个干    | A→B→C        | 任务按顺序在 Agent 间流转，像流水线，例如dify                                                            |
| Hierarchical 分层 | 老板分配，员工干活 | 调度者→执行者们     | 特点 ：一个"管理者"Agent 负责任务分配和协调，其他 Agent 各司其职。     <br/>代表 ：AutoGen、CrewAI 的 hierarchical 模式 |
| Discussion 讨论   | 开会讨论出结论   | A↔B↔C 互相质疑   | 特点 ：多个 Agent 通过对话讨论来解决问题，互相质疑和完善。 <br/>      代表 ：AutoGen 的群聊模式                          |
| Swarm 群体        | 各干各的，汇总结果 | 并行执行→合并      | 特点 ：大量简单 Agent 并行工作，涌现出集体智能。<br/> 代表 ：OpenAI Swarm、Oh My Claude Code 的 Swarm 模式         |

三、执行模式（Agent 怎么"跑"）

| 模式            | 一句话           | 适用      |
|---------------|---------------|---------|
| Autopilot 全自动 | 给目标，自己搞定      | 确定性重复任务 |
| Copilot 人机协作  | 人拍板，AI干活      | 日常开发辅助  |
| Ultrapilot 并行 | 3-5个同时干，快     | 大规模任务   |
| Ecomode 经济    | 简单用小模型，复杂用大模型 | 省成本     |

四、记忆模式（Agent 怎么"记"）

| 模式   | 特点           |
|------|--------------|
| 无状态  | 用完即忘，适合一次性任务 |
| 短期记忆 | 记住当前对话，关了就忘  |
| 长期记忆 | 向量库持久化，跨会话记住 |

## 零基础入门

### 1.技术准备

```
1. 必备编程基础：只需要掌握 Python 基础 即可开始：

学习优先级：
★★★ 变量、数据类型、条件判断、循环
★★★ 函数定义与调用
★★  字典、列表操作
★★  文件读写
★   类与对象（面向对象基础）
★   异步编程（async/await，进阶再学）


2. 推荐学习路径（4 周速成）
| 周次 | 学习内容 | 资源推荐 | 目标 |
|------|---------|---------|------|
| 第1周 | Python 基础语法 | [Python 官方教程](https://docs.python.org/zh-cn/3/tutorial/) / 廖雪峰 Python 教程 | 能写简单脚本 |
| 第2周 | 函数 + 数据结构 | LeetCode 简单题（每天1-2题） | 熟练操作数据 |
| 第3周 | API 调用 + JSON | requests 库实战 | 能调用外部 API |
| 第4周 | 面向对象 + 类 | 理解 class 基本用法 | 能组织代码结构 |

3. 必备工具清单
| 工具 | 用途 | 安装方式 |
|------|------|---------|
| Python 3.10+ | 编程语言 | python.org 下载 |
| VS Code | 代码编辑器 | code.visualstudio.com |
| Git | 版本管理 | git-scm.com |
| pip / uv | 包管理器 | Python 自带 / `pip install uv` |
| OpenAI API Key | LLM 访问 | platform.openai.com 注册 |

```

### 2.调用大模型

直接使用openai的SDK调用大模型

```python
案例1：直接使用openai的SDK调用大模型
from openai import OpenAI
client = OpenAI(api_key="你的key",
                base_url="https://platform.aitools.cfd/api/v1")

response = client.chat.completions.create(
    model="zhipu/glm-4-flash",
    messages=[
      {"role": "system", "content": "你是张三的AI助手"},
      {"role": "user", "content": "你好，你是谁？"},
    ],
    stream=False
)
print(response.choices[0].message.content)


案例2：最简单的 Agent
目标：理解 Agent 的基本循环 — "感知→思考→行动"
关键理解：这个"最简 Agent"已经包含了感知（接收输入）和思考（LLM推理），只是还没有记忆和工具。

from openai import OpenAI

client = OpenAI(api_key="你的API密钥")

def simple_agent(user_input: str) -> str:
    """最简单的 Agent：接收输入 → LLM 思考 → 返回输出"""
    
    system_prompt = """你是一个有帮助的AI助手。
    回答问题时，先分析用户需求，再给出清晰的结构化回答。"""
    
    response = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_input}
        ]
    )
    
    return response.choices[0].message.content

# 测试
result = simple_agent("我想学习Python，应该从哪里开始？")
print(result)



案例3：添加记忆的 Agent
目标：让 Agent 能记住之前的对话
关键理解：`self.messages` 就是 Agent 的短期记忆。每次对话都带上完整历史，LLM 就能"记住"之前的内容。

from openai import OpenAI
client = OpenAI(api_key="你的API密钥")

class MemoryAgent:
  def __init__(self):
    # 定义系统提示器，用于初始化记忆
    # 注意：这里只是使用系统内存保存上下文。
    self.messages = [
      {"role": "system", "content": "你是一个有帮助的AI助手，能记住之前的对话内容。"}
    ]

  def chat(self, user_input: str) -> str:
    # 感知：记录用户输入
    self.messages.append({"role": "user", "content": user_input})

    # 思考：LLM 推理（自动包含历史记忆）
    response = client.chat.completions.create(
        model="zhipu/glm-4-flash",
        messages=self.messages
    )

    assistant_message = response.choices[0].message.content

    # 记忆：保存助手回复
    self.messages.append({"role": "assistant", "content": assistant_message})

    return assistant_message

  def show_memory(self):
    """查看 Agent 的记忆"""
    print(f"记忆中有 {len(self.messages)} 条消息")
    for msg in self.messages[1:]:  # 跳过 system prompt
      role = "👤用户" if msg["role"] == "user" else "🤖助手"
      print(f"{role}: {msg['content'][:50]}...")

# 测试
agent = MemoryAgent()
print(agent.chat("我叫小明，我在学Python"))
print(agent.chat("我叫什么名字？"))  # Agent 能记住！
agent.show_memory()



案例4：带工具的 Agent
目标：让 Agent 能调用外部工具，从"只会说话"变成"能做事"
关键理解：这就是 ReAct 模式 的核心 — LLM 自主决定何时调用工具、调用哪个工具、如何使用工具结果。

import json
from openai import OpenAI

client = OpenAI(api_key="sk-31132f3d71114bc3bd904ea90e117982",
                base_url="https://platform.aitools.cfd/api/v1")
# 定义工具函数
def get_weather(city: str) -> str:
  """模拟天气查询（实际项目中调用真实API）"""
  weather_data = {
    "北京": "晴天，25°C",
    "上海": "多云，22°C",
    "深圳": "小雨，28°C"
  }
  return weather_data.get(city, f"{city}：暂无天气数据")

def calculate(expression: str) -> str:
  """安全计算数学表达式"""
  try:
    allowed = set("0123456789+-*/.() ")
    if all(c in allowed for c in expression):
      return str(eval(expression))
    return "不安全的表达式"
  except:
    return "计算错误"

# 工具注册表
tools = [
  {
    "type": "function",
    "function": {
      "name": "get_weather",
      "description": "查询指定城市的天气",
      "parameters": {
        "type": "object",
        "properties": {
          "city": {"type": "string", "description": "城市名称"}
        },
        "required": ["city"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "calculate",
      "description": "计算数学表达式",
      "parameters": {
        "type": "object",
        "properties": {
          "expression": {"type": "string", "description": "数学表达式"}
        },
        "required": ["expression"]
      }
    }
  }
]

# 工具执行映射
tool_map = {
  "get_weather": get_weather,
  "calculate": calculate
}

class ToolAgent:
  def __init__(self):
    self.messages = [
      {"role": "system", "content": "你是一个能查询天气和做计算的AI助手。根据用户需求选择合适的工具。"}
    ]

  def chat(self, user_input: str) -> str:
    self.messages.append({"role": "user", "content": user_input})

    # 第一步：LLM 决定是否需要调用工具
    response = client.chat.completions.create(
        model="openai/gpt-oss-20b",
        messages=self.messages,
        tools=tools
    )

    message = response.choices[0].message

    # 第二步：如果 LLM 决定调用工具
    if message.tool_calls:
      for tool_call in message.tool_calls:
        func_name = tool_call.function.name
        func_args = json.loads(tool_call.function.arguments)

        print(f"  🔧 调用工具: {func_name}({func_args})")

        # 执行工具（使用  展开字典为关键字参数）
        result = tool_map[func_name](func_args)
        print(f"  📋 工具结果: {result}")

        # 把工具结果告诉 LLM
        self.messages.append(message)
        self.messages.append({
          "role": "tool",
          "tool_call_id": tool_call.id,
          "content": result
        })

      # 第三步：LLM 基于工具结果生成最终回答
      final_response = client.chat.completions.create(
          model="openai/gpt-oss-20b",
          messages=self.messages
      )
      final_message = final_response.choices[0].message.content
      self.messages.append({"role": "assistant", "content": final_message})
      return final_message

    # 不需要工具，直接回答
    self.messages.append({"role": "assistant", "content": message.content})
    return message.content

# 测试
agent = ToolAgent()
print(agent.chat("北京今天天气怎么样？"))
print("---")
print(agent.chat("帮我算一下 (123 + 456) * 2"))



案例5：多步骤规划 Agent
目标：Agent 能自主拆解复杂任务，分步执行
本质：提示词工程

from openai import OpenAI

client = OpenAI(api_key="sk-31132f3d71114bc3bd904ea90e117982",
                base_url="https://platform.aitools.cfd/api/v1")

class PlanningAgent:
  """能自主规划多步骤任务的 Agent"""

  def __init__(self):
    self.messages = [
      {"role": "system", "content": """你是一个智能规划助手。
当收到复杂任务时，你会：
1. 先制定执行计划（列出步骤）
2. 逐步执行每个步骤
3. 每步完成后评估结果
4. 如有偏差，调整计划

请用以下格式输出计划：
📋 计划：
1. [步骤1]
2. [步骤2]
...

然后逐步执行。"""}
    ]
    self.max_iterations = 5  # 防止无限循环

  def run(self, task: str) -> str:
    self.messages.append({"role": "user", "content": task})

    for i in range(self.max_iterations):
      response = client.chat.completions.create(
          model="openai/gpt-oss-20b",
          messages=self.messages
      )

      reply = response.choices[0].message.content
      self.messages.append({"role": "assistant", "content": reply})

      print(f"\n--- 迭代 {i+1} ---")
      print(reply)

      # 检查是否完成
      check = client.chat.completions.create(
          model="openai/gpt-oss-20b",
          messages=self.messages + [
            {"role": "user", "content": "任务是否已完成？只回答'是'或'否'"}
          ]
      )

      if check.choices[0].message.content.strip().startswith("是"):
        print("\n✅ 任务完成！")
        return reply

      # 继续执行
      self.messages.append({"role": "user", "content": "请继续执行下一步。"})

    return "⚠️ 达到最大迭代次数"

# 测试
agent = PlanningAgent()
agent.run("帮我分析学习Python的完整路线图，包括每个阶段的学习重点和时间安排")

```

---

### 3.框架与平台

主流框架对比

| 框架              | 适合人群      | 特点              | 学习难度 |
|-----------------|-----------|-----------------|------|
| LangChain       | 最广泛的入门选择  | 生态丰富、组件多、文档全    | ⭐⭐   |
| CrewAI          | 团队协作场景    | 多Agent角色编排、简洁易用 | ⭐⭐   |
| AutoGen         | 微软出品      | 多Agent对话、研究导向   | ⭐⭐⭐  |
| Semantic Kernel | .NET/企业开发 | 微软生态、企业级        | ⭐⭐⭐  |
| Dify            | 低代码/快速搭建  | 可视化编排、部署方便      | ⭐    |
| Coze            | 零代码入门     | 扣子平台、拖拽式        | ⭐    |

### 4.LangChain 快速入门

```python
# 安装：pip install langchain langchain-openai

from langchain.agents import create_agent
from langchain_core.tools import tool

from _5_机器学习._04_langchain.common import model_openai


# 第一步：定义工具
@tool
def search_movie(title: str) -> str:
  """搜索电影信息"""
  movies = {
    "星际穿越": "2014年，导演：克里斯托弗·诺兰，评分：9.4",
    "盗梦空间": "2010年，导演：克里斯托弗·诺兰，评分：9.3"
  }
  return movies.get(title, "未找到该电影")

@tool
def recommend_movie(genre: str) -> str:
  """根据类型推荐电影"""
  recs = {
    "科幻": "推荐：《星际穿越》《银翼杀手2049》《降临》",
    "喜剧": "推荐：《布达佩斯大饭店》《功夫》《让子弹飞》"
  }
  return recs.get(genre, "暂无该类型推荐")

# 第二步：创建 Agent（LangChain 1.0 新 API）
tools = [search_movie, recommend_movie]
agent = create_agent(
    model=model_openai,
    tools=tools,
    system_prompt="你是一个电影推荐助手，能搜索电影信息和根据类型推荐电影。"
)

# 第三步：运行
result = agent.invoke({
  "messages": [{"role": "user", "content": "我想看一部科幻电影，帮我搜索星际穿越的信息"}]
})

for msg in result["messages"]:
  if hasattr(msg, "content") and msg.content:
    print(f"[{msg.type}] {msg.content}")
```

### 5.简单的Agent

```python
# 技巧1：开启 verbose 模式查看 Agent 思考过程
agent_executor = AgentExecutor(agent=agent, tools=tools, verbose=True)

# 技巧2：打印完整消息链
for msg in agent.messages:
    print(f"[{msg['role']}] {msg['content'][:100]}")

# 技巧3：限制迭代次数防止死循环
agent_executor = AgentExecutor(
    agent=agent, 
    tools=tools, 
    max_iterations=5,  # 最多5轮
    early_stopping_method="generate"  # 超限后生成最终回答
)

# 技巧4：添加重试机制
import time
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(stop=stop_after_attempt(3), wait=wait_exponential(min=1, max=10))
def call_llm_with_retry(messages):
    return client.chat.completions.create(
        model="gpt-4o-mini",
        messages=messages
    )
```

## 进阶

### 1.扩展能力
构建AI Agent，建议按顺序安装和配置的 5 个核心工具，目标：系统性解决开发中常见的安全、成本与调试痛点。
1. 隐私（密钥管理）：使用 direnv 配合真正的密钥管理器（如 1Password CLI），实现基于项目的环境变量自动加载与卸载，确保 API 密钥等凭证永不明文存储在磁盘上，从根本上防止密钥泄露。
2. 成本与路由（模型代理）：通过 litellm 或 Portkey 等工具代理所有 AI 模型供应商的调用。这能实现响应缓存以大幅节省账单、遇限流自动降级、设置预算护栏防止意外开销，并智能路由任务到性价比最优的模型。
3. 可复现性（依赖与上下文管理）：采用快速的 Python 包管理器 uv 锁定精确依赖。建议每次评估套件（eval suite）通过后都进行一次 Git 提交，并在提交信息中记录模型版本和通过率，从而确保任何历史运行状态都可精确复现和追溯。
4. 可见性与调试（流量监控）：在 Agent 的所有 LLM 调用前部署 mitmproxy。它可以实时窥探 Agent 与模型之间的完整对话、实际 token 成本，并能及时发现隐藏在响应中的意外指令或 Prompt 注入攻击，让开发者真正了解 Agent 的行为。
5. 评估（标准化测试框架）：使用专业评估框架 inspect-ai（被多家顶尖研究机构使用）来系统化评估 Agent。它能提供可重复、可比较的量化分数，并包含对高风险行为的预设测试，其评估结果比自行编写的测试工具更具可信度和可复现性。

## LangChain

- 官网：[https://www.langchain.com/](https://www.langchain.com/)
- 国内文档：[https://docs.langchain.org.cn/](https://docs.langchain.org.cn/)
    - 包括 LangChain、LangGraph、LangSmith、Deep Agents等。

注意：LangChain 1.0.0rc2 是一个全新的大版本，API 完全重构了。

LangChain 是代理工程平台。Replit、Clay、Rippling、Cloudflare、Workday 等公司的 AI 团队都信赖 LangChain 的产品来工程化可靠的代理。

1. LangChain：LangChain 代理是基于 LangGraph 构建的，以提供持久执行、流式处理、人工干预、持久性等功能，而LangChain提供更加灵活自主的程序。
2. LangGraph：通过低级编排、内存和人机协作支持来控制自定义代理的每一步。
3. LangSmith：是一个帮助 AI 团队利用实时生产数据进行持续测试和改进的平台，LangSmith 提供：
    - 可观测性，通过详细的追踪和聚合趋势指标，准确了解您的代理如何思考和行动。
    - 评估，对生产数据和离线数据集上的代理行为进行测试和评分，以实现持续改进。
    - 部署，通过为长时间运行任务构建的可扩展基础设施，一键发布您的代理。

### 1.安装

```shell
# langchain 核心
pip install -U langchain

# LangChain 提供了与数百个 LLM 和数千个其他集成的集成。这些集成存在于独立的提供商包中。例如：
pip install -U langchain-deepseek
pip install -U langchain-openai
pip install -U langchain-anthropic
```

### 2.核心组件

#### 2.1.模型

大型语言模型（LLM）是强大的 AI 工具，能够像人类一样解释和生成文本。它们功能多样，可以撰写内容、翻译语言、总结和回答问题，而无需针对每个任务进行专门训练。

除了文本生成，许多模型还支持：
1. 工具调用 - 调用外部工具（如数据库查询或 API 调用）并在其响应中使用结果。
2. 结构化输出 - 模型响应被限制为遵循定义的格式。
3. 多模态 - 处理和返回除文本之外的数据，例如图像、音频和视频。
4. 推理 - 模型执行多步推理以得出结论。

langchain提供：动态模型、工具调用、输出格式化、多模态、提示缓存、限流、URL代理、日志概率、token用量、
调用配置（使用langsmith监控调试、日志监控、资源使用等）

#### 2.2.消息

消息包括输入与输出，消息是包含以下内容的对象：
1. 角色 - 识别消息类型（例如 system、user）
2. 内容 - 表示消息的实际内容（如文本、图像、音频、文档等）
3. 元数据 - 可选字段，如响应信息、消息 ID 和令牌使用情况

#### 2.3.工具

工具是langchain调用并执行操作的组件，工作流程：
1. 通过模型明确的输入和输出。
2. langchain工具封装了一个可调用函数及其输入模式，这些可以传递给兼容的聊天模型，允许模型决定是否调用工具以及使用哪些参数。
3. 在这些场景中，工具调用使模型能够生成符合指定输入模式的请求。


访问上下文：工具可以通过 ToolRuntime 参数访问运行时信息
1. 上下文：通过 runtime.context 访问不可变配置和上下文数据，如用户 ID、会话详细信息或应用程序特定配置。
2. 内存（存储）：使用存储跨对话访问持久数据。存储通过 runtime.store 访问，允许您保存和检索用户特定或应用程序特定数据。
3. 流写入器：使用 runtime.stream_writer 在工具执行时流式传输自定义更新。这对于向用户提供有关工具正在做什么的实时反馈非常有用。

#### 2.4.记忆

记忆上下文，包括内存、Redis、数据库等。

#### 2.5.流式

LangChain 流式传输可能实现的功能：
1. 流式传输 agent 进度 — 在每个 agent 步骤之后获取状态更新。
2. 流式传输 LLM Tokens — 在语言模型生成 token 时进行流式传输。
3. 流式传输自定义更新 — 发送用户定义的信号（例如，"已获取 10/100 条记录"）。
4. 流式传输多种模式 — 从 updates (agent 进度)、messages (LLM token + 元数据) 或 custom (任意用户数据) 中选择。

#### 2.6.中间件

1. 监控：使用日志、分析和调试跟踪代理行为
2. 修改：转换提示、工具选择和输出格式
3. 控制：添加重试、回退和提前终止逻辑
4. 强制执行：应用速率限制、防护栏和 PII 检测

langchain内置很多中间件。
1. 总结：在接近令牌限制时自动总结对话历史。SummarizationMiddleware
2. 人工干预：在工具调用执行之前，暂停代理执行，以进行人工批准、编辑或拒绝。
3. Anthropic 提示缓存：通过 Anthropic 模型缓存重复的提示前缀来降低成本。
4. 模型调用限制：限制模型调用次数，以防止无限循环或过高成本。
5. 工具调用限制：限制对特定工具或所有工具的调用次数。
6. 模型回退：当主模型失败时，自动回退到备用模型。
7. PII 检测：检测和处理对话中的个人身份信息。
8. 规划：为复杂的、多步骤任务添加待办事项列表管理功能。
9. LLM 工具选择器：在调用主模型之前，使用 LLM 智能选择相关工具。
10. 工具重试：自动重试失败的工具调用，并可配置指数退避。
11. LLM 工具模拟器：使用 LLM 模拟工具执行进行测试，用 AI 生成的响应替换实际的工具调用。
12. 上下文编辑：通过修剪、总结或清除工具使用来管理对话上下文。


自定义中间件：通过实现代理执行流程中特定点的钩子来构建自定义中间件。方式如下：
1. 基于装饰器 - 适用于单钩子中间件，快速简单
2. 基于类 - 适用于具有多个钩子的复杂中间件，功能更强大

#### 2.7.结构化输出

响应格式：控制代理如何返回结构化数据
1. ToolStrategy[StructuredResponseT]：使用工具调用进行结构化输出
2. ProviderStrategy[StructuredResponseT]：使用提供商原生结构化输出
3. type[StructuredResponseT]：架构类型 - 根据模型能力自动选择最佳策略
4. None：无结构化输出

### 3.最佳实践

#### 3.1.安全防护
通过在代理执行的关键点验证和过滤内容，帮助你构建安全、合规的 AI 应用程序。 常见用例包括：
1. 防止 PII 泄漏。主要对如电子邮件、信用卡、IP、Mac、url等进行脱敏，策略包括编辑、掩码、哈希、阻止等。
2. 检测并阻止提示注入攻击
3. 阻止不适当或有害内容
4. 强制执行业务规则和合规性要求
5. 验证输出质量和准确性

实现原理：通过中间件，在关键节点（agent执行前后、工具调用前后等）进行拦截处理。

#### 3.2.MCP

模型上下文协议 (MCP) 是一个开放协议，用于标准化应用程序向大型语言模型 (LLM) 提供工具和上下文的方式。

通过@mcp.tool自定义mcp服务。

```shell
pip install langchain-mcp-adapters
```

#### 3.3.multi-agent
agent间协同完成复杂任务。 agent间通过工具调用或者控制权交接实现任务执行。

实现原理：通过主agent控制各个子agent的执行

核心：上下文工程。需要控制的内容如下
1. 对话或状态的哪些部分传递给每个智能体。
2. 为子智能体量身定制的专用提示。
3. 包含/排除中间推理。
4. 为每个智能体定制输入/输出格式。


#### 3.4.检索-RAG

|架构	| 描述                            | 	控制 | 	灵活性 | 	延迟	 | 示例用例           |
|---|-------------------------------|-----|------|------|----------------|
|两步RAG| 检索总是在生成之前发生。简单且可预测            | ✅高  | ❌低   | ⚡ 快  | 常见问题解答、文档机器人   |
|代理式RAG| 一个由 LLM 驱动的代理决定在推理过程中何时以及如何检索 | ❌低  | ✅高   | ⏳可变  | 可访问多种工具的研究助手   |
|混合| 结合了两种方法的特点，并带有验证步骤            | ⚖️中 | ⚖️中  | ⏳可变  | 带有质量验证的领域特定问答  |

#### 3.5.Studio
Studio 是langchain提供的免费、强大的代理 IDE，它与 LangSmith 集成，可实现跟踪、评估和提示工程。
精确查看您的代理如何思考，跟踪每一个决策，并发布更智能、更可靠的代理。

#### 3.6.集成测试
LangChain提供的AgentEvals通过执行轨迹匹配或使用LLM 判断器实现智能体的轨迹的评估（确切的消息序列，包括工具调用）。

```shell
pip install agentevals
```
1. 轨迹匹配评估器。AgentEvals 提供了 create_trajectory_match_evaluator 函数，用于将您的智能体轨迹与参考轨迹进行匹配。
2. LLM-as-Judge 评估器。可以使用 LLM 和 create_trajectory_llm_as_judge 函数来评估智能体的执行路径。与轨迹匹配评估器不同，它不需要参考轨迹，但如果可用可以提供。
3. 异步支持。所有 agentevals 评估器都支持 Python asyncio。对于使用工厂函数的评估器，通过在函数名中的 create_ 后面添加 async 即可获得异步版本。

#### 3.7.LangSmith 集成

为了跟踪随时间变化的实验，您可以将评估器结果记录到 LangSmith，这是一个用于构建生产级 LLM 应用程序的平台，包括跟踪、评估和实验工具。

LangSmith 提供两种主要的评估方法：pytest 集成和 evaluate 函数。

## LangGraph

LangGraph 受到包括 Klarna、Replit、Elastic 等在内的塑造未来智能体的公司的信任，是一个用于构建、管理和部署长时间运行、有状态智能体的低级编排框架和运行时。

LangGraph 是非常低级的，完全专注于智能体编排。普遍使用LangChain 组件来集成模型和工具。

LangGraph 专注于对智能体编排至关重要的底层功能：持久执行、流式传输、人机协作等。

```shell
pip install -U langgraph
```

## AgentScope

官网：[https://java.agentscope.io/zh/intro.html](https://java.agentscope.io/zh/intro.html)

AgentScopes是阿里达摩院为Java 17+生态打造的开源AI Agent生产级框架。

是专为分布式、企业级智能体 打造的 Harness 框架

重点解决了类型安全、生产可控性、生态集成等核心需求，是当前Java生态中少有的、为生产环境设计的AI Agent框架。

### 1. 背景与定位
- 背景：当前AI Agent领域的主流框架（如LangChain、AutoGen）多基于Python，Java开发者若想构建具备自主决策能力的Agent，在技术选型和架构集成上常面临挑战。
    Spring AI虽提供了LLM集成，但更侧重于将模型作为工作流中的一个节点，而非构建自主Agent。
  - 定位：AgentScope Java是专为Java生态设计的AI Agent开发框架，旨在让Java开发者能够便捷地构建生产级别的LLM应用。
  它由阿里团队维护，是Python版AgentScope理念的Java移植，并深度集成了Maven、响应式编程、GraalVM等Java生态特性。

### 2. 核心特性
- 推理范式：采用ReAct（Reasoning and Acting） 范式。与固定流程的框架不同，ReAct让Agent在“观察-推理-行动”的循环中动态规划任务路径，能更好地处理意外情况。
- 生产级控制：
    - 运行时干预：支持安全中断、优雅取消，Agent状态可保留与恢复。
    - Hook系统（Human-in-the-Loop）：允许在任意推理步骤插入人工审核或干预，适用于敏感操作场景。
- 内置工具链：
    - PlanNotebook：结构化任务管理，支持Agent自主创建、追踪和修改计划。
    - 结构化输出：自动将模型输出映射为Java POJO，并具备格式自纠错能力。
    - 长期记忆：支持持久化、基于语义搜索的记忆存储，具备多租户隔离能力。
    - RAG集成：内置检索增强生成支持，可对接企业知识库或云服务（如阿里云百炼）。
- 协议与生态：
    - MCP协议：原生支持Anthropic的Model Context Protocol，可无缝接入大量实现了MCP服务端的工具（如数据库、浏览器）。
    - A2A协议：支持Agent-to-Agent通信，可通过服务注册中心（如Nacos）实现多Agent、跨语言（如Java与Python）的微服务式协作。

### 3. 生产就绪能力
- 高性能架构：基于Project Reactor的响应式编程，支持高并发与非阻塞执行；支持GraalVM原生镜像编译，将冷启动时间降至200ms级别，适合Serverless与K8s环境。
- 安全与可观测性：
    - 安全沙箱：为工具代码提供隔离执行环境，防止Agent越权操作。
    - 可观测性：集成OpenTelemetry，支持全链路分布式追踪；提供AgentScope Studio可视化调试界面，便于开发调试与生产监控。

### 4.与Spring AI的关系
- 定位差异：Spring AI将LLM作为工作流中的一个可控节点，适合在既定业务流程中嵌入AI能力；AgentScope Java则将LLM视为自主决策的“大脑”，适合路径不固定、需Agent动态规划的场景。
- 互补可能：两者可协同使用，例如用Spring AI管理主流程，在需要自主决策的环节调用由AgentScope Java构建的Agent。

## Pi Agent

官网：[https://pi.dev/](https://pi.dev/)
开源：[https://github.com/earendil-works/pi](https://github.com/earendil-works/pi)

Pi Agent（简称 Pi）是一个极简、可高度定制的开源 AI 编码智能体/Agent 框架，由 Mario Zechner（libGDX 作者）于 2024 年推出，
现在是热门开源项目 OpenClaw 的核心引擎。  

它主打：内核极小、工具极少、扩展极强、模型无关。

Pi Agent = 极简内核 + 4 个原子工具 + 全模型兼容 + 无限扩展，是当前最火的开源 AI 编程智能体，
OpenClaw 的底层大脑，适合做轻量、可控、可定制的自动化任务与编程助手。

### 1.PI是什么
- 定位：终端 AI 编程智能体 + Agent 运行时框架
- 开源：GitHub 仓库 `badlogic/pi-mono`
- 别名：pi-coding-agent、pi-agent-core、OpenClaw 大脑

### 2.核心设计哲学：极简到极致
#### 2.1内核只做 4 件
也就是只有 4 个内置工具
- read：读文件/代码上下文
- write：新建文件/写代码
- edit：精准修改文件
- bash：执行终端命令（编译、运行、部署）

> 没有“计划模式、子 Agent、审批流程”等花活——把复杂度交给扩展，核心只留最薄执行层。

#### 2.2系统提示词极短
- 对比 Claude Code / Cursor：动辄 8000–15000 token
- 好处：上下文利用率高、速度快、成本低、不易跑偏

#### 2.3模型无关，统一接口
- 支持 OpenAI、Anthropic、Google、xAI、Groq、Ollama、vLLM 等 15+ 大模型
- 统一 LLM API：换模型只需改配置，不用重写适配层

### 3.架构（pi-mono 5 个包）
1. pi-ai：统一 LLM 接口（多模型、流式、计费）
2. pi-agent-core：Agent 循环、工具执行、事件流（核心大脑）
3. pi-coding-agent：终端 CLI（交互、会话持久、主题）
4. pi-tui：轻量终端 UI（响应式、无闪烁）
5. pi-web-ui：Web 管理界面（可选）

### 4.适用场景
1. 终端 AI 编程助手：直接在命令行写代码、改 Bug、跑测试、部署
2. 自动化运维：服务器操作、日志分析、脚本执行
3. 构建自定义 Agent：通过 TypeScript 扩展/技能包，添加新工具、新流程
4. 嵌入到其他系统：作为 OpenClaw、IDE 插件、内部平台的底层引擎

### 5.和 Claude Code / Cursor 的区别
- Pi Agent：极简内核、4 工具、短提示词、高度可扩展、终端优先、开源
- Claude Code：功能大而全、内置规划/子 Agent、提示词极长、闭源
- Cursor：IDE 深度集成、图形界面、闭源、定制化弱

### 6.简单使用
```bash
# 安装
npm install -g --ignore-scripts @earendil-works/pi-coding-agent

# 运行（默认用 OpenAI GPT-5-mini）
pi-agent "写一个 Python 快速排序"

# 切换模型
pi-agent --model ollama:codellama "解释这段代码"
```

Package Catalog：https://pi.dev/packages



